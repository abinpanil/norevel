{
  "name": "@norevel/cli",
  "version": "0.0.0",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "rm -rf dist",
    "lint": "echo \"lint placeholder\"",
    "test": "echo \"test placeholder\"",
    "dev": "tsx src/cli.ts",
    "watch": "tsx watch src/cli.ts",
    "serve:watch": "tsx watch src/cli.ts serve"
  },
  "bin": {
    "norevel": "dist/cli.js"
  },
  "dependencies": {
    "@norevel/http": "workspace:*",
    "@norevel/core": "workspace:*",
    "@norevel/orm": "workspace:*"
  }
}
